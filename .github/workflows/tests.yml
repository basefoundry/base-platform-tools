name: Tests

on:
  push:
  pull_request:

jobs:
  validate:
    name: Repository validation
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run validation
        run: tests/validate.sh
